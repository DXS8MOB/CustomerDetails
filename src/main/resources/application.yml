server:
  port: 8087
  servlet:
    context-path: /orderservice

spring:
  datasource:
     driver-class-name: org.postgresql.Driver
     url: jdbc:postgresql://localhost:5432/dipali1
     password: Talkhed@1996
     username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
users:
  url: http://localhost:8082/customerservice/getCustomer/orderId/